source "https://rubygems.org"

# 指定 github-pages 版本，自动匹配对应 Jekyll 版本
gem "github-pages", "232", group: :jekyll_plugins
gem "jekyll", "3.10.0"

# 仅保留必要依赖
gem "jekyll-feed", "~> 0.15"
gem "jekyll-seo-tag", "~> 2.8"

# 开发环境依赖
group :development do
  gem "wdm", ">= 0.1.0" if Gem.win_platform?
end